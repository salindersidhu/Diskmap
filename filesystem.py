class FileSystem(object):

    def __init__(self, path, size, colour):
        '''Create a new FileSystem with path, size and colour.'''
        self.path = path
        self.size = size
        self.colour = colour
